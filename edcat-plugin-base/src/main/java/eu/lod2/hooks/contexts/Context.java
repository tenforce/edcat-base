package eu.lod2.hooks.contexts;

/**
 * A Context is an object which is supplied to the implementer of a handler.
 *
 * These contexts make the evolution of handler interfaces easier to maintain.
 * It is now possible to add information without breaking the specific
 * HookHandler interface.
 */
@SuppressWarnings( "UnusedDeclaration" )
public class Context {
}
